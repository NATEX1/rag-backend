/**
 * Hooks index file following barrel pattern for internal hooks.
 * This is acceptable since it's internal to the project.
 */

export { useHealth, useStats, useAskQuestion, useUploadDocument, KEYS } from './useApi';
